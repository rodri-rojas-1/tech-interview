# Frontend

Angular (or chosen SPA) app will live here.

Create the project when ready, for example from the repo root:

```bash
cd frontend
npx @angular/cli@latest new . --routing --style=scss --ssr=false
```

Point the dev server proxy at the TaskManager API (see root `README.md`).
