export const environment = {
  production: false,
  /** Dev: empty = same origin; `ng serve` proxies /api → API HTTPS (see proxy.conf.json; must match UseHttpsRedirection, no 307 to browser) */
  apiBaseUrl: '',
};
